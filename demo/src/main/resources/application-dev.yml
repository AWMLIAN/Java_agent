spring:
  datasource:
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}

langchain4j:
  open-ai:
    chat-model:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: "https://api.deepseek.com"
      model-name: "deepseek-chat"
      log-requests: true
      log-responses: true
    embedding-model:
      api-key: ${SILICONFLOW_API_KEY}
      base-url: https://api.siliconflow.cn/v1/
      model-name: BAAI/bge-m3
      log-requests: true
      log-responses: false
  ollama:
    embedding-model:
      enabled: false



jwt:
  secret: ${JWT_SECRET:bXlfamF2YV9hZ2VudF9teV9qYXZhX2FnZW50}
  expiration: 604800
  tokenHead: 'Bearer '

logging:
  level:
    com.example.demo: DEBUG